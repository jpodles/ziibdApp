@page
@model ziibdApp.Pages.JobHist.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>JobHistory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="JobHistory.EmployeeId" />
            <input type="hidden" asp-for="JobHistory.StartDate" />
            <div class="form-group">
                <label asp-for="JobHistory.EndDate" class="control-label"></label>
                <input asp-for="JobHistory.EndDate" class="form-control" />
                <span asp-validation-for="JobHistory.EndDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="JobHistory.JobId" class="control-label"></label>
                <select asp-for="JobHistory.JobId" class="form-control" asp-items="ViewBag.JobId"></select>
                <span asp-validation-for="JobHistory.JobId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="JobHistory.DepartmentId" class="control-label"></label>
                <select asp-for="JobHistory.DepartmentId" class="form-control" asp-items="ViewBag.DepartmentId"></select>
                <span asp-validation-for="JobHistory.DepartmentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
